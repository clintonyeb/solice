{
  "text": "{{text}}",
  "photo": "{{photo}}",
  "_id": "{{_id}}",
  {{#if likes.length}}
      "likes": "{{likes}}",
  {{/if}}
  {{#if comments.length}}
      "comments": {{comments}},
  {{/if}}
  "postedBy": "{{postedBy}}",
  "created": "{{created}}",
}
